# Postgres connection string (Neon/Heroku style). REQUIRED.
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Base URL of your deployed app (used for UI copy links). Example:
BASE_URL=http://localhost:3000

# Optional: port for local start
PORT=3000
